<h1>Listing councils</h1>

<table>
  <tr>
    <th>Councilname</th>
    <th>Counciltel</th>
    <th>Councilemail</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @councils.each do |council| %>
  <tr>
    <td><%= council.CouncilName %></td>
    <td><%= council.CouncilTel %></td>
    <td><%= council.CouncilEmail %></td>
    <td><%= link_to 'Show', council %></td>
    <td><%= link_to 'Edit', edit_council_path(council) %></td>
    <td><%= link_to 'Destroy', council, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Council', new_council_path %>
